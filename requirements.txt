# Install the package and all dependencies:
#   pip install -e .
#
# Or install dependencies directly:
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
gemmi>=0.6.0
huggingface-hub>=0.22.0
numpy>=1.24.0
biopython>=1.81
tqdm>=4.65.0

# ESM3 must be installed separately:
#   pip install git+https://github.com/evolutionaryScale/esm.git
